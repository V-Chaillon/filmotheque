{% extends 'base.html.twig' %}

{% block title %}Film{% endblock %}

{% block body %}
    <div class="title-form">
        <h3>Détail d'un Film</h3>
    </div>

    <table class="table">
        <tbody>
        <tr>
            <th>Titre</th>
            <td>{{ film.titre }}</td>
        </tr>
        <tr>
            <th>Resume</th>
            <td>{{ film.resume }}</td>
        </tr>
        <tr>
            <th>DateSortie</th>
            <td>{{ film.dateSortie ? film.dateSortie|date('d/m/Y') : '' }}</td>
        </tr>
        <tr>
            <th>Personnage</th>
            <td>
                {% for personnages in film.personnages %}
                    {{personnages.nom}}
                {% endfor %}
            </td>

        </tr>
        </tbody>
    </table>

    <a href="{{ path('film_edit', {'id': film.id}) }}">Modifier</a>
    {{ include('film/_delete_form.html.twig') }}


{% endblock %}