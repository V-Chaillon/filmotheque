<div class="form">
    <div class="main-form">
        {{ form_start(form) }}
        <div class="input">
            {{ form_label(form.nom) }}
            {{ form_widget(form.nom) }}
        </div>
        <div class="button-form">
            <button class="btn">{{ button_label|default('Ajouter') }}</button>
        </div>
        {{ form_errors(form) }}
        {{ form_end(form) }}
    </div>
</div>